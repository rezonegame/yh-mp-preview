# yh-mp-preview 双场景排版升级计划

状态：执行中
基线：v3.8.2 / 4ac4120
许可证：AGPL-3.0-or-later
正式验收平台：Windows；macOS 未完成实测前不宣称已支持

## 1. 目标与边界

插件最终同时服务两个场景：

1. 保持微信公众号预览、主题、配方、复制、HTML、长图和分段图稳定，并持续修复导出与兼容问题。
2. 用户首次开启笔记排版增强后，Obsidian 阅读视图和实时预览自动使用增强样式；源码模式可单独启用基础显示优化。

笔记增强默认关闭，开启一次后自动恢复。增强只改变显示，不修改 Markdown、frontmatter、链接、图片路径或空格。公众号设置与笔记设置分开保存。

本计划不包含 AI、大模型、图片生成、云同步、在线主题市场或第二套富文本编辑器。现有七个文章场景和十四个主题保留，不增加颜色重复卡片。

## 2. 固定产品规则

笔记设置优先级如下：总开关关闭时使用 Obsidian 原生显示；开启后，笔记级“原生排版”优先；没有笔记级配置时使用全库默认。笔记级配置保存完整的主题、字号、行高和最大宽度，不做逐字段继承。

默认笔记参数为 `default` 主题、宿主字体、16px 字号、1.75 行高和 760px 最大宽度。允许字号 14–24px、行高 1.4–2.2、最大宽度 560–960px。颜色跟随宿主明暗模式，公众号颜色保持独立。

主题画廊分为“笔记阅读”和“公众号”两个场景。卡片只显示名称和选中状态，推荐用途显示在底部。笔记模式支持试用、应用到当前笔记、设为全库默认、使用原生排版；关闭画廊取消未应用试用。自定义主题首轮只用于公众号。

笔记增强必须使用局部 CSS 类和变量，不能污染侧栏、设置、弹窗、Canvas 或公众号导出快照。阅读视图使用 Obsidian 注册机制，实时预览使用宿主 CodeMirror 扩展，不轮询全库 DOM，不重建公式、Mermaid、嵌入或第三方渲染内容。

独立设置文件 `note-layout.json` 使用版本化结构。未知新版本或损坏文件不得被默认值覆盖；迁移前必须备份并读回校验。文件重命名和文件夹移动同步路径覆盖；删除文件移除覆盖；跨库复制使用全库默认。

## 3. 版本路线

每个版本按 `beta → rc → stable` 推进；前一阶段未通过门禁不得晋级。

| 版本 | 交付内容 | 退出条件 |
| --- | --- | --- |
| 3.8.3 | 导出资源超时、快照清理、分段进度、锁文件和预发布门禁 | 自动门禁通过，预发布可由 BRAT 指定安装 |
| 3.9.0 | 独立笔记设置、迁移备份、恢复入口、增强模块骨架 | 升级、回退、重载不丢旧设置，不产生残留 |
| 3.10.0 | 阅读视图和实时预览基础自动增强，先支持三主题 | 输入、选择、撤销、折叠和重启正常 |
| 3.11.0 | 列表、任务、引用、Callout、代码、表格、图片及源码基础显示 | 复杂 Markdown 编辑行为保持原生 |
| 3.12.0 | 十四主题、双场景画廊、笔记级和全库应用 | 试用取消、路径迁移、多视图同步通过 |
| 3.13.0 | 共享框架元数据和公众号跨场景应用优化 | 公众号输出与既有功能不退化 |
| 3.14.0 | 性能、宿主兼容、文档和稳定收口 | RC 三个实际使用日及全部发布门禁通过 |

当前 A 阶段预发布为 `3.8.3-beta.1`，B 阶段预发布为 `3.9.0-beta.1`，C 阶段 `3.10.0-beta.2` 和 D 阶段 `3.11.0-beta.1` 已通过真实验收，现进入 E 阶段 `3.12.0-beta.1`。各阶段均需完成真实 Obsidian/BRAT 验收后，才允许分别进入 RC。

## 4. 统一门禁

每个版本必须执行：

```text
npm ci
npm run verify
npm run build
npm run check:release -- --tag <实际标签> --require-build
git diff --check
```

测试分四层：纯函数和迁移测试、浏览器导出测试、独立 Obsidian 测试库验收、微信后台人工粘贴验收。源码正则测试只能作为结构检查，不能替代实际行为测试。

固定测试内容覆盖标题、长段落、中英混排、列表、任务、Callout、代码、宽表格、本地和远程图片、双链、嵌入、脚注、公式、Mermaid，以及首中尾标记的超长文章。

发布工作流必须将包含连字符的标签识别为预发布，不设为 latest；稳定标签才成为 latest。发布完成必须核实 Release、三个插件发行文件、许可证、版本和校验值。标签推送成功不等于发布完成。

## 5. 回退规则

显示问题：关闭笔记增强，确认原生显示恢复，公众号继续使用。设置迁移前备份 `data.json` 和 `note-layout.json`，保存版本、时间和 SHA-256；首次基线永久保留，另保留最近十次有效迁移备份。设置写入采用临时文件、读回校验和替换；失败时保留原文件。

程序回退：保存笔记并停用插件，通过 BRAT 指定已知稳定版本；必要时只恢复 `main.js`、`manifest.json`、`styles.css`，不删除整个插件目录。回退程序不自动改写笔记正文。

遇到笔记损坏、中文输入异常、插件无法加载、导出缺失、设置无法恢复，立即停止晋级。没有实际宿主或人工验收结果时只能标记“待人工验收”。

## 6. 执行顺序

先维护本文件、`docs/dual-layout/EXECUTION_LOG.md` 和 `docs/dual-layout/ACCEPTANCE.md`，再执行当前版本代码任务。每项任务记录基线提交、修改、命令退出码、证据、回退版本和完成提交。不得覆盖用户工作区改动，不得移动已有稳定标签。
