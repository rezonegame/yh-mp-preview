# 双场景排版升级执行日志

## 当前基线

- 仓库：`yh-mp-preview`
- 执行基线：`v3.8.2` / `4ac4120`
- 当前阶段：RC / 3.14.0
- 当前候选：`v3.14.0-rc.1`（待发布，等待三个实际使用日观察）
- 平台重点：Windows

## A 阶段：导出与发布稳定性

状态：已验收（用户确认）

已完成：

- `package.json`、`manifest.json`、`versions.json`、`package-lock.json` 版本同步。
- 发布检查覆盖锁文件版本和许可证。
- GitHub Release 根据标签是否含连字符自动区分预发布。
- 长图和分段图等待图片资源，超时为 10 秒。
- 快照创建失败自动清理临时节点。
- 超长单张长图无法安全生成时提示改用分段图。
- 分段图显示当前进度，失败时提示已完成数量。
- 自动测试、主题审计、构建和发布元数据检查通过。

验收结果：

- 在独立 Obsidian 测试库用 BRAT 安装 `v3.8.3-beta.1`。
- 用超过预览窗口高度的测试文章导出长图和分段图，检查首、中、尾内容。
- 改变预览窗口高度和滚动位置后重复导出，结果应完整一致。
- 使用失效图片验证超时提示和临时节点清理；再次使用正常文章应可导出。
- 从 `v3.8.2` 回退并重新安装，确认公众号功能正常。

## B 阶段：3.9.0-beta.1

状态：已验收（用户确认）

已完成：

- 新增独立 `note-layout.json`，默认关闭笔记排版增强。
- 新增 schema 版本检查、字段范围归一化和未知新版本保护。
- 新增临时文件、读回校验和替换流程。
- 新增首次基线及最近十次备份保留策略。
- 新增设置页开关、手动备份、最近备份恢复和命令面板入口。
- 新增笔记增强生命周期边界；3.9.0 不改变正文视觉样式。
- 公众号渲染链路保持原有结构，24 项自动测试和全部审计通过。

验收结果：

- 新库首次启动后确认没有 `note-layout.json` 时公众号功能正常。
- 开关一次后重启，确认独立设置文件存在且开关状态恢复。
- 手动创建备份、修改开关、恢复最近备份，确认状态恢复。
- 模拟设置文件损坏或未来 schema，确认只禁用笔记增强，不影响公众号功能。
- 停用和重新启用插件，确认没有残留设置页错误或视觉改动。
- 从 `v3.9.0-beta.1` 回退至 `v3.8.2`，确认公众号主题、配方和导出正常。

## C 阶段：3.10.0-beta.2

状态：已验收（用户确认）

已完成：

- 阅读视图注册 Markdown 后处理器，按笔记路径应用局部主题类和阅读变量。
- 实时预览注册 CodeMirror 6 扩展，按当前文件应用同一组阅读变量。
- 首批支持 `default`、`deep-reading`、`minimal` 三套主题。
- 设置页提供主题、字号、行高和最大宽度，修改后刷新已打开笔记。
- 开关变化会刷新阅读预览和编辑器扩展，关闭后清理自身类和变量。
- 样式限定在 `.yh-mp-note-layout` 与 `.cm-editor.yh-mp-note-layout`，不进入公众号预览样式。
- 25 项自动测试、主题审计、构建和发布校验通过。
- beta.2 修复 Live Preview 编辑区被阅读最大宽度压窄的问题。

验收结果：

- 在 Obsidian 阅读视图开启增强，确认三套主题和四项参数生效。
- 在实时预览输入中文、选择文字、撤销重做、折叠标题，确认编辑行为正常。
- 切换阅读、实时预览和源码模式，确认 Markdown 内容不被改写。
- 切换明暗主题、窄窗口、多标签和重启，确认设置自动恢复。
- 关闭增强，确认原生显示恢复，公众号预览不发生变化。

## D 阶段：3.11.0-beta.1

状态：已验收（用户确认）

已完成：

- 阅读视图补充列表、任务、引用、Callout、代码、表格、图片和嵌入的局部样式。
- 源码模式开关正式可用，仅增强 CodeMirror 标记的层次和可读性。
- 源码模式和阅读样式均通过笔记增强边界应用，不改写 Markdown 内容。
- 关闭增强或笔记级使用原生排版时清理新增类和变量。

验收结果：

- 阅读视图逐项检查列表、任务、引用、Callout、代码、表格、图片和嵌入。
- 源码模式开启后检查标题、列表、引用、强调、链接和代码标记，确认输入、选择、撤销重做正常。
- 检查宽表格和大图在窄窗口下可滚动且不撑破 Obsidian 页面。
- 关闭源码模式、关闭总开关和切换笔记级原生排版，确认显示恢复且公众号链路无变化。

## E 阶段：3.12.0-beta.1

状态：已验收（用户确认）

已完成：

- 保留公众号主题画廊为独立场景，标题和场景栏明确标识公众号用途。
- 新增笔记阅读主题画廊，提供默认阅读、深度阅读和极简阅读三套主题。
- 主题卡片只显示主题名称和选中状态，推荐作用统一显示在底部。
- 支持卡片试用、应用到当前笔记、设为全库默认和当前笔记恢复原生排版。
- 试用使用临时覆盖，关闭画廊自动清除，不写入笔记设置。
- 当前笔记覆盖保存到独立 `note-layout.json`，与公众号设置和主题选择分离。

验收结果：

- 从笔记排版设置打开画廊，确认当前笔记主题试用即时生效，取消后恢复原样。
- 应用到当前笔记、设为全库默认、使用原生排版后重启 Obsidian，确认状态保持。
- 在不同笔记、重命名和移动文件后确认覆盖范围正确，不影响其他笔记。
- 从公众号预览打开公众号主题画廊，确认主题、配方、试用和应用流程不变。

## F 阶段：3.13.0-beta.1

状态：已验收（用户确认）

已完成：

- 主题目录为公众号和笔记阅读提供统一的框架 ID、适用场景、推荐作用和 surfaces 元数据。
- 三套笔记主题从共享目录读取名称、推荐作用和框架关系，不再在画廊内重复维护。
- V3 ThemeManifest 支持框架、场景、推荐作用和适用 surface 元数据，并保留旧主题兼容路径。
- 自定义主题默认只进入公众号 surface，不会自动进入笔记画廊。
- 笔记设置继续只写入 `note-layout.json`，公众号主题继续只写入插件设置，跨场景应用边界明确。

验收结果：

- 公众号画廊应用主题后，笔记主题和 `note-layout.json` 不发生变化。
- 笔记画廊应用主题后，公众号当前主题、配方和导出结果不发生变化。
- 导入带元数据的 ThemeManifest 后，公众号画廊可正常显示和应用；旧版主题仍可加载。

## G 阶段：3.14.0-beta.1

状态：已验收（用户确认）

已完成：

- 笔记文件重命名、移动时同步 `note-layout.json` 中的路径覆盖。
- 笔记删除时清理对应路径覆盖，避免设置文件持续增长。
- Live Preview 只在文档或视口变化时重新应用样式，选择变化不再重复写类和变量。
- 为引用和 Callout 增加不支持 `color-mix()` 时的基础回退样式。
- 新增 Windows/Obsidian 宿主边界、设置生命周期和回退说明。
- 28 项自动测试、主题审计、公众号基线和构建检查通过。

验收结果：

- 重命名、移动和删除带笔记级主题覆盖的文件，确认覆盖同步或移除。
- 多标签、重启、关闭增强和回退版本后确认公众号链路不受影响。
- 按宿主兼容说明检查阅读视图、Live Preview、源码模式和主题画廊。

## RC 阶段：3.14.0-rc.1

状态：待发布

进入条件：

- G 阶段已通过用户验收。
- 不增加新功能，只同步候选版本和发布元数据。
- 自动测试、构建、主题审计、公众号兼容性基线和发布门禁全部通过。

观察期：

- 在真实 Obsidian/BRAT 测试库连续使用三个实际使用日。
- 期间重点记录插件加载、笔记排版、主题画廊、文件生命周期、公众号预览和导出问题。
- 出现阻塞问题则回退到 `v3.14.0-beta.1`，修复后重新发布 RC。

## 记录模板

```text
任务 ID / 版本：
状态：未开始 | 进行中 | 待人工验收 | 阻塞 | 完成
基线提交：
修改摘要：
验证命令与退出码：
实际宿主与测试结果：
证据位置：
回退版本与备份：
未解决问题：
完成提交：
```
